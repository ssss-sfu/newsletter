<div style="
padding-left: 2%;
padding-right: 2%;
">
    <p style="font-family: 'Dm Sans', 'Trebuchet MS', Consolas, Arial, sans-serif, monospace;
    line-height: 1.5;
    color: #564DC2;
    font-weight: 700;
    font-style: italic;" class="detail">{{ subheader }}</p>
    <p style="font-family: 'Dm Sans', 'Trebuchet MS', Consolas, Arial, sans-serif, monospace;
    line-height: 1.5;">{{ text }}</p>

    <div style="margin-top: 0.5em;">
    {% for link in links %}
        {% set vars.linkCount = vars.linkCount + 1%}
        {% include "link.jinja" %}
    {% endfor %}
    </div>
</div>